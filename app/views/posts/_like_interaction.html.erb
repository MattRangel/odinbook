<p>
<span class="like-heart">
  <% if post.liked_by_id?(current_user.id) %>
    <%= "💖" %>
  <% else %>
    <%= link_to "❤️", likes_path(like:{post_id: post.id}, authenticity_token: form_authenticity_token), data: { turbo_method: :post } %>
  <% end %>
</span>
<%= pluralize(post.likes.length, 'like') %>
</p>
